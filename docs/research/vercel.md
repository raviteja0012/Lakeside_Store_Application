# Vercel: feature surface

Researched 2026-08-12 against the vendor's live documentation, not from memory. 73 capabilities.

**This is a reference catalog, not a plan.** What we adopt and what we decline, with reasons,
lives in [CAPABILITIES.md](../CAPABILITIES.md). The services actually wired up today live in
[PLATFORM.md](../PLATFORM.md). Re-check anything here before acting on it: vendor pricing and
feature names move, and this is a dated snapshot.

Scope note from the researcher:

> Vercel


## AI

| Capability | What it does | Plan or cost | Maturity |
|---|---|---|---|
| **AI Gateway** | A single endpoint and API key that routes to hundreds of models across providers with automatic retries and cross-provider failover, load balancing, embeddings, image/video generation, web search, and BYOK. Works with AI SDK v5/v6, OpenAI Chat Completions and Responses, and Anthropic Messages APIs. | No markup on tokens — you pay provider rates, including with Bring Your Own Key. Observability, managed fallback, load balancing, spend monitoring, app attribution, and automatic retries are included on all plans. Billed from a prepaid AI Gateway credit balance viewable via GET /v1/credits. | GA |
| **AI SDK** | Open-source TypeScript toolkit ('ai' package) giving a unified provider API, streaming text/objects/UI, schema-validated structured outputs via generateObject/streamObject, and tool calling. Works with React, Next.js, Vue, Svelte, and Node.js. | Free and open source; no Vercel plan requirement. Model tokens are billed by whichever provider or by AI Gateway. | GA (v5 and v6) |
| **Vercel Agent** | An in-dashboard agent that investigates production incidents, answers questions from deployments/logs/metrics/config, reviews pull requests with Sandbox-validated patches, and can take approved write actions such as opening a PR or rolling back. Read-only by default; @vercel mentions in PR comments trigger it. | Pro and Enterprise only, public beta with gradual rollout. Credit-based: $0.30 per review or investigation plus underlying token cost at provider rates with no markup. Agent Installation (auto-adding Web Analytics/Speed Insights) is free for all teams. Agent Investigation requires Observability Plus. | public beta |
| **eve** | An open-source, filesystem-first framework for durable backend AI agents where an agent/ directory of instructions, tools, skills, and channels compiles into a runnable app. On Vercel it runs on Functions and wires into Workflows (session state), Sandbox (isolation), AI Gateway (models), Connect (credentials), and Observability (agent runs). | Framework is free/open source; usage bills through the underlying Vercel resources it consumes (Functions, Workflows, Sandbox, AI Gateway, Connect). Subject to Vercel's beta terms. | beta |
| **v0** | Vercel's prompt-to-UI AI development assistant with Design Mode, GitHub sync, and one-click deploy to Vercel. Billed separately from the core Vercel platform via a token-based credit balance. | Separate product/billing at v0.app/pricing: Free $0 with $5 monthly credits and 7 messages/day; Plus $30/user/month; Business $100/user/month (both include $30/user monthly credits plus $2/day login credits); Enterprise custom with SAML SSO and RBAC. Purchased credits expire after one year; monthly credits roll over and expire after 65 days. Note: Vercel's own pricing page lists v0 as a 'paid add-on' for Pro/Enterprise. | GA |

## AI / Developer Tooling

| Capability | What it does | Plan or cost | Maturity |
|---|---|---|---|
| **MCP Server Hosting** | Lets you build and deploy your own MCP servers on Vercel so LLM hosts can call your tools and data over a standard interface. Documented alongside AI SDK helpers for initializing MCP clients and calling MCP tools. | No separate product fee; billed as normal Vercel Function usage on any plan. | GA |
| **Vercel MCP (official MCP server)** | Vercel's own remote MCP server at https://mcp.vercel.com with OAuth, exposing tools to search docs, manage projects and deployments, analyze logs, and query Web Analytics. Supported clients include Claude Code, Claude.ai/desktop, ChatGPT, Codex CLI, Cursor, VS Code Copilot, Devin, Raycast, Goose, Windsurf, and Gemini. | No separate charge documented; access is scoped to the connecting user's Vercel account permissions. Only Vercel-reviewed AI clients can connect. | GA |

## AI / Integrations

| Capability | What it does | Plan or cost | Maturity |
|---|---|---|---|
| **Vercel Connect** | Issues short-lived, per-project and per-environment provider tokens at runtime so agents can act on third-party APIs without long-lived secrets in env vars. Supports Vercel-managed connectors (Slack, GitHub, Snowflake, Salesforce) and customer-managed ones (Custom OAuth 2.0/OIDC, API key), plus installations, token subject types (app/user/jwt-bearer), refresh/revocation, and verified webhook trigger forwarding. | Billed per token request. Hobby: 5,000 token requests/month included. Pro and Enterprise: $3 per 10,000 token requests. Connect event retention beyond plan window requires a Drain (Pro/Enterprise). | beta |

## Analytics

| Capability | What it does | Plan or cost | Maturity |
|---|---|---|---|
| **Speed Insights** | Real-user Core Web Vitals monitoring (Real Experience Score, FCP, LCP, INP and more) with P75/P90/P95/P99 views, a routes/paths/element kanban of what needs improvement, and a per-country map. Tracks all deployed environments including preview and production. | Hobby: 10,000 events/month included. Pro: $10 per project per month plus $0.65 per 10,000 data points. Enterprise custom. Reporting window length varies by plan. | GA |
| **Web Analytics** | Cookieless, privacy-friendly visitor analytics with visitors, page views, bounce rate, and panels for top pages, referrers, country, OS, device, browser, custom events, and feature flag usage. Visitors are identified by a daily-rotating request hash rather than cookies, and bot traffic is excluded by user-agent inspection. | Hobby: 50,000 events/month included. Pro: $3 per 100,000 events. Web Analytics Plus: $10/month on Pro (available on Enterprise). Enterprise custom. CSV export limited to 250 entries per panel. | GA |

## Builds

| Capability | What it does | Plan or cost | Maturity |
|---|---|---|---|
| **Build Concurrency & On-Demand Concurrent Builds** | Lets queued builds skip the queue and run in parallel, configurable team-wide or per project (run all immediately vs one build per branch). Also includes Prioritize Production Builds so production jumps ahead of preview builds in the FIFO queue. | Hobby: 1 concurrent deployment. Pro: up to 500 concurrent with on-demand enabled (3 if off), billed by build minutes consumed. Enterprise: custom concurrency. | GA |
| **Build Machines (Standard / Enhanced / Turbo / Elastic)** | Selects the compute size used to run builds: Standard 4 vCPU/8 GB/32 GB disk, Enhanced 8/16/64, Turbo 30/60/64, and Elastic which auto-scales 4-30 vCPU based on the project's real workload. Elastic is the default for new paid teams. | Pro/Enterprise only (Hobby gets Standard, unbilled). Standard $0.014/min, Enhanced $0.028/min, Turbo $0.105/min, Elastic $0.0035 per CPU-minute (a CPU minute = build minutes x vCPU cores). | GA |
| **Remote Cache & Monorepo Support** | Shares Turborepo build artifacts across CI and team members to skip redundant work, with first-class monorepo project detection. Runtimes also retain up to 100 MB of build-time filesystem cache keyed by project/team/entrypoint/runtime version. | Remote Cache and Monorepo Support included on all plans at no extra cost. | GA |

## CDN & Caching

| Capability | What it does | Plan or cost | Maturity |
|---|---|---|---|
| **Bulk Redirects** | Stores large redirect maps at the edge so you can ship tens of thousands of redirects without bloating your route config or redeploying. Managed at the project level. | Not available on Hobby. Pro: 1,000 redirects per project included, then $50 per 25,000. Enterprise custom. | GA |
| **CDN Cache, Runtime Cache & Purge** | Layered caching beyond ISR: per-region CDN response caching driven by Cache-Control headers (including for external-origin rewrites), plus a Runtime Cache for fetch results, DB queries, and computed values inside functions. Content can be purged manually, programmatically, or by tag across all frameworks and backends. | Included on all plans. CDN cache reads/writes are free; you pay only Edge Requests and data transfer. Blobs are CDN-cached up to 1 month by default; static files up to 31 days. | GA |
| **Image Optimization** | Dynamically resizes, re-encodes (WebP/AVIF), and caches images requested through framework Image components via /_next/image or /_vercel/image, honoring remotePatterns and localPatterns allowlists. Cache MISS/STALE bills a transformation plus a cache write; a global-cache HIT bills a cache read. | Hobby included: 5K transformations, 300K cache reads, 100K cache writes/month. Pro on-demand: $0.05 per 1K transformations, $0.40 per 1M cache reads, $4 per 1M cache writes. Enterprise custom. Teams must opt in to the transformation-based pricing model in Billing settings. | GA |
| **Incremental Static Regeneration (ISR)** | Serves cached pages while regenerating in the background via time-based or on-demand revalidation, with durable per-deployment storage in your Function region, cache shielding, automatic request collapsing, and globally consistent purge within 300 ms. Supported in Next.js, SvelteKit, Nuxt, Astro, Gatsby (DSG), and the Build Output API. | Hobby included: 1M ISR reads, 200K ISR writes. Pro on-demand: $0.40 per 1M reads, $4 per 1M writes, measured in 8 KB read/write units and regionally priced. No storage cap; content persists 31 days unaccessed. Function invocations and Fast Origin Transfer billed separately. | GA |
| **Partial Prerendering (PPR) / Cache Components** | Serves a cached static shell instantly from the CDN while Functions render and stream the dynamic holes into the same response. In Next.js you enable Cache Components and mark cached segments with 'use cache', controlling freshness with cacheLife and invalidation with cacheTag. | No separate SKU: billed as ISR reads/writes + Function invocations (most PPR requests incur an invocation even on a shell cache hit) + Fast Origin Transfer. Available on all plans. Observability exposes shell hit rate and dynamic invocations. | GA as of Next.js 16 (no longer experimental); currently Next.js only |
| **Vercel CDN (Delivery Network)** | Framework-aware global network of 126 PoPs across 51 countries fronting 20+ compute regions, handling routing, redirects/rewrites, TLS, compression, load balancing, and automatic region failover over a private backbone. Caching policy is derived from your build output rather than hand-written Cache-Control headers. | Edge Requests: 1M/mo Hobby, 10M/mo Pro then $2 per 1M. Fast Data Transfer: 100 GB Hobby, 1 TB Pro then $0.15/GB. Fast Origin Transfer: 10 GB Hobby, then $0.06/GB on Pro. Pricing varies by request region. Enterprise custom. | GA |

## Collaboration

| Capability | What it does | Plan or cost | Maturity |
|---|---|---|---|
| **Comments on Deployments** | Point-and-click or text-highlight comment threads on preview and other environments, with email notifications to PR owners and thread participants and optional linking to Slack threads. Requires the Toolbar to be active and every commenter to have a Vercel account. | Enabled by default on all preview deployments for all plans, free of charge. Inviting external collaborators (Previewers) to comment requires Pro or Enterprise; Previewer seats are unlimited and free on Pro/Enterprise. | GA |
| **Draft Mode** | Bypasses the ISR cache using a build-time bypassToken and a __prerender_bypass cookie so team members can view unpublished headless CMS content in the real site layout. Supported by Next.js and SvelteKit, or any framework via the Build Output API prerender bypassToken. | Included on all plans at no cost. Only team members can enable it; sharing a draft URL requires ?__vercel_draft=1 and viewers outside the team still cannot see draft content. | GA |
| **Edit Mode / Content Link** | Highlights CMS-backed fields directly on the rendered page via content source maps so authors can jump straight into the matching CMS field to edit. Requires no page-level code changes and works with Contentful, Sanity, Builder, TinaCMS, DatoCMS, Payload, Uniform, and Strapi. | Included with the Vercel Toolbar on all plans; no separate charge. Requires a supported CMS integration that emits content source maps. | GA |
| **Vercel Toolbar** | An in-browser overlay on preview (and optionally production/localhost) deployments providing comments, flags, Draft Mode, Edit Mode, branch switching, Open Graph preview, and share links. Also hosts the Layout Shift tool, Interaction Timing (INP) tool, and the WCAG 2.0 A/AA Accessibility Audit tool. | Free on all plans and enabled by default on preview deployments. A browser extension adds screenshots, production toolbar access, and Always Activate/Start Hidden preferences. | GA |

## Compute

| Capability | What it does | Plan or cost | Maturity |
|---|---|---|---|
| **Container Images on Functions** | Runs OCI container images (Dockerfile.vercel / Containerfile.vercel) as Vercel Functions, autoscaling on demand and scaling to zero after 5 minutes idle in production (30s in preview). Containers get SIGTERM with a 30-second grace period and serve HTTP on port 80 by default. | Same limits and Active CPU pricing as regular Vercel Functions. Not yet supported with Secure Compute or Static IPs. | beta |
| **Function Max Duration** | Caps how long a single invocation may run before a 504 FUNCTION_INVOCATION_TIMEOUT, configurable per function via maxDuration. Vercel points workloads needing unlimited time at Workflows instead. | Hobby: 300s default and max. Pro/Enterprise: 300s default, 800s maximum (GA), 1800s (30 min) extended maximum in beta and requires per-function config. Edge runtime: must start responding within 25s, can stream up to 300s. No separate charge; duration only affects metered CPU/memory. | GA (800s); extended 1800s is beta |
| **Function Runtimes** | Official runtimes are Node.js, Bun, Python (ASGI/WSGI: FastAPI, Flask, Django), Go, Ruby, Rust, Wasm, and Edge (V8 isolates). Community runtimes cover Deno, PHP, and Bash, and you can build your own via the Runtime API or Build Output API. | All runtimes included on all plans; billed identically under fluid compute pricing. Fluid optimized concurrency applies to Node.js and Python. | GA |
| **Large Functions** | Raises the uncompressed function bundle limit from 250 MB (500 MB Python) to 5 GB for workloads shipping big dependencies, model files, or binaries. Enabled by default on new projects; existing projects opt in with VERCEL_SUPPORT_LARGE_FUNCTIONS=1. | Requires fluid compute with Active CPU. No separate charge documented; billed as normal function usage. Not supported with Secure Compute or Static IPs. | beta |
| **Streaming Functions** | Streams responses to the client in chunks as they are produced, which is the standard pattern for LLM output. Node.js supports it by default, Python is supported, and waitUntil lets background work continue after the response. | Included on all plans; billed as normal function usage. Streaming can't run past the plan's max duration (300s Hobby, 800s Pro/Ent). | GA |
| **Vercel Container Registry (VCR)** | A Docker Registry HTTP API v2 / OCI-compatible private registry at vcr.vercel.com scoped per Vercel project, used to store images for Functions and Sandbox. Repositories are private by default and can be shared with named teams or made public. | Available on all plans. Image storage is billed separately per /docs/container-registry/limits-and-pricing — I did not fetch that page, so the exact GB-month rate is unverified. | GA |
| **Vercel Functions (Fluid compute)** | Server-side compute that autoscales to zero, with Fluid compute sharing one instance across concurrent invocations so I/O wait doesn't burn CPU billing, plus bytecode caching and pre-warming to cut cold starts. Limits: 2 GB/1 vCPU (Hobby) or 4 GB/2 vCPU (Pro/Ent), 250 MB bundle (500 MB Python), 4.5 MB request/response body, 1,024 file descriptors, autoscale to 30,000 concurrency (100,000+ Enterprise). | Hobby includes 4 Active CPU hours, 360 GB-hrs Provisioned Memory, 1M invocations/mo. Pro on-demand and regionally priced: iad1/pdx1/cle1 $0.128 per Active CPU hour and $0.0106 per GB-hr; most expensive is gru1 at $0.221/$0.0183. Invocations $0.60 per 1M. Enterprise custom. | GA |

## Compute / AI Infrastructure

| Capability | What it does | Plan or cost | Maturity |
|---|---|---|---|
| **Vercel Sandbox** | Ephemeral Firecracker microVMs for running untrusted or AI-generated code, with root access, Docker-in-sandbox, multi-agent Linux users, persistent sandboxes with auto-snapshot, and Drives for reusable filesystem storage. JS and Python SDKs plus a CLI; default image vercel/sandbox/universal ships Node LTS, Python 3.14, and coding agents. | Hobby includes 5 Active CPU hrs, 420 GB-hrs memory, 5,000 creations, 20 GB transfer, 15 GB snapshot storage. Pro/Ent: $0.128/Active CPU hr, $0.0212/GB-hr, $0.60/1M creations, $0.15/GB network, $0.08/GB-month snapshots. Max runtime 45 min Hobby vs 24 hrs Pro/Ent; concurrency 10 vs 10,000; max 4 vCPU/8 GB Hobby, 8/16 Pro, 32/64 Enterprise. Only available in iad1. | GA (persistence GA); Drives in beta |

## Compute / Durable Execution

| Capability | What it does | Plan or cost | Maturity |
|---|---|---|---|
| **Vercel Workflows** | Fully managed durable execution for JavaScript, TypeScript, and Python using 'use workflow' and 'use step' directives, so code can sleep, wait on hooks, and resume across deploys and crashes for minutes to months. Runs on Functions, orchestrated by Queues, with managed persistence, streams, multi-region run pinning, and built-in tracing. | Hobby: 50,000 Workflow Events and 1 GB Data Written included; Data Retained not available. Pro on-demand: $0.02 per 1K events (i.e. $20/1M), $0.50/GB written, $0.50/GB-month retained. Storage retention after completion: 1 day Hobby, 7 days Pro, 30 days Enterprise. Underlying Function and Queue usage billed separately. | GA product; Workflow SDK multi-region requires workflow v5.0.0-beta.33+ |

## Compute / Messaging

| Capability | What it does | Plan or cost | Maturity |
|---|---|---|---|
| **Vercel Queues** | Durable append-only topic-based event streaming with independent consumer groups, retries, visibility timeouts, idempotency keys, and push or poll consumption. It is the lower-level primitive that powers Workflows. | Hobby: 1M API operations/month included. Pro: $0.60 per 1M operations, regionally priced. Messages metered in 4 KiB chunks; idempotent sends and max-concurrency push deliveries bill at 2x. Limits: 100 MB max message, TTL 60s-7 days (24h default), 60 min max visibility timeout. | GA |

## Compute / Scheduling

| Capability | What it does | Plan or cost | Maturity |
|---|---|---|---|
| **Cron Jobs** | Schedules HTTP GET requests to a path on your production deployment on a UTC 5-field cron expression, defined in vercel.json or the Build Output API. Requests carry a vercel-cron/1.0 user agent and an x-vercel-cron-schedule header. | Included on all plans; you only pay for the underlying Function invocation. 100 cron jobs per project on every plan. Hobby is limited to once per day with per-hour precision (±59 min); Pro/Enterprise allow once per minute with per-minute precision. | GA |

## Configuration

| Capability | What it does | Plan or cost | Maturity |
|---|---|---|---|
| **Environment Variables & Secrets** | Encrypted-at-rest key/value config scoped to Production, Preview (optionally per-branch), Custom Environments, and Development, settable at team or project level and pullable locally with `vercel env pull`. Integrations can inject their own variables, and secret rotation is documented for zero-downtime credential swaps. | Included on all plans. 64 KB total per deployment across all variables (Node.js, Python, Ruby, Go, PHP community runtime); Edge runtime functions and middleware are capped at 5 KB per variable. Changes apply only to new deployments. | GA |

## Developer Experience

| Capability | What it does | Plan or cost | Maturity |
|---|---|---|---|
| **Claim Deployments** | Generates a claim URL (vercel.com/claim-deployment?code=…) that lets an end user take ownership of a deployment created by an agent or another account, transferring the project into their chosen team. Associated Marketplace resources from Neon, Supabase, and Prisma transfer with it. | Included on all plans at no charge; built for AI-generated deployments and agent-to-human handoff. Transfer codes are valid for 24 hours. For same-owner team moves, Vercel recommends the normal Project Transfer flow instead. | GA |
| **Feature Flags (Vercel Flags, Flags SDK, Flags Explorer)** | A complete flags platform where Vercel can be your flag provider or you connect a Marketplace provider (LaunchDarkly, Statsig, etc.), with one unified dashboard, the open Flags SDK, and the Toolbar's Flags Explorer for local overrides without code changes. Flag evaluations show up in Runtime Logs and can be segmented in Web Analytics. | Vercel Flags Requests: 10,000/month included on Hobby; $0.03 per 1,000 on Pro. Flags Explorer: 150 overrides/month on all plans, with unlimited overrides available for $250/month on Pro and Enterprise. Enterprise custom. | GA |
| **Open Graph (OG) Image Generation** | The @vercel/og library (built on Satori + Resvg) renders HTML/CSS into PNG social cards from a Vercel Function, with automatic CDN cache headers. Supports flexbox layouts, custom ttf/otf/woff fonts, Google Fonts subsetting, emoji, SVG, and experimental Tailwind. | Free library; you pay only the underlying Function invocation. Supported on the Node.js runtime (bundled into Next.js App Router). Limits: 500 KB max bundle including fonts and images, flexbox only (no CSS grid). | GA |
| **Vercel CLI, REST API & SDK** | The `vercel` CLI covers deploy, dev, env, blob, connect, integration/install, promote, rolling-release, vcr, and list-with-retention-policy operations. The REST API and @vercel/sdk expose projects, deployments, drains, firewall/WAF, rolling releases, and rollback endpoints, with Terraform support for firewall config. | Free on all plans; access tokens are created from account settings. API rate limits apply per team (e.g. 100 domain additions/hour). | GA |

## Domains

| Capability | What it does | Plan or cost | Maturity |
|---|---|---|---|
| **Domains & DNS** | Buy, transfer, or delegate domains and manage DNS records (A, CNAME, NS, MX and more) or Vercel nameservers, with automatically provisioned SSL/TLS certificates and wildcard domain support. A registrar REST API allows programmatic domain management. | Domain management, DNS, and automatic SSL are included on all plans; domain registration/renewal is priced per TLD (rates not published on the docs pages I fetched — verify in the dashboard). Custom (bring-your-own) SSL certificates are Enterprise-only. API rate limits: 100 domain adds/hour, 50 verifications/hour, 100 removals/hour per team. | GA |

## Edge & Routing

| Capability | What it does | Plan or cost | Maturity |
|---|---|---|---|
| **Routing Middleware** | Runs code before a request hits the cache so you can rewrite, redirect, add headers, or personalize static content globally. Defaults to the Edge runtime and can be switched to Node.js or Bun via a config export. | Included on all plans; billed under the fluid compute model (Active CPU + Provisioned Memory + invocations). Request limits: 14 KB URL, 4 MB body, 64 headers, 16 KB header length. Edge-runtime env vars capped at 5 KB each. | GA |

## Governance & Billing

| Capability | What it does | Plan or cost | Maturity |
|---|---|---|---|
| **Spend Management** | Sets a per-billing-cycle dollar cap on metered resources that can fire web/email/SMS notifications at 50/75/100%, POST to a webhook, and optionally auto-pause production deployments on all team projects. Paused projects return 503 DEPLOYMENT_PAUSED and must be resumed individually. | Free feature, Pro plan, requires Owner or Billing role. Does not cover seats, Marketplace integrations, or monthly add-ons. Spend is checked only every few minutes, so usage can overshoot the cap before pausing takes effect. | GA |

## Governance & Security

| Capability | What it does | Plan or cost | Maturity |
|---|---|---|---|
| **Audit Logs** | Records team member activity (deployments, domains, env vars, integrations, rolling releases, protection changes, Passport access grants, member changes) with actor, IP, user agent, request ID, and before/after state. Exportable as CSV via a 24-hour signed link, or streamed to a SIEM. | Enterprise plan only, viewable by Owners. Exports covering the last 90 days do not affect billing. Custom SIEM Log Streaming (S3, Splunk, Datadog, GCS, generic HTTP POST) is being replaced by Audit Log Drains. | GA (SIEM streaming being migrated to Drains) |
| **Compliance & HIPAA BAA** | Vercel maintains SOC 2 Type 2, ISO 27001, PCI DSS, TISAX AL2, and EU-U.S. Data Privacy Framework attestations covering the platform. A HIPAA Business Associate Agreement is available as a paid option for teams handling PHI. | SOC 2 Type 2, PCI DSS, ISO 27001, EU-U.S. DPF, and TISAX AL2 are included on all plans. HIPAA BAA: $350/month on Pro, custom on Enterprise. Custom security questionnaires: Enterprise only. | GA |
| **RBAC & Access Groups** | Assigns team roles (Owner, Member, Developer, Billing, Viewer, Security, Contributor and project administrators) that gate who can deploy, promote, roll back, or change firewall and billing settings. Access Groups bundle project-level permissions such as Full Production Deployment. | Not available on Hobby. Pro: team-level roles only. Enterprise: team-level and project-level roles plus access groups. Seat pricing: Developer seat $20/month, Viewer seats unlimited and free on Pro, 1 Billing seat, unlimited Previewers. | GA |
| **SAML SSO & Directory Sync (SCIM)** | Authenticates team members through Okta, Auth0, Google, Microsoft Entra, ADFS, OneLogin, PingFederate, Keycloak and ~20 other IdPs, with optional enforcement so no non-SAML session can access team data. Directory Sync automatically provisions and de-provisions members and roles from your directory. | SAML SSO: $300/month add-on on Pro (purchase from Billing settings before configuring); included on Enterprise. Directory Sync (SCIM): Enterprise only. SSO-Protected Previews: Enterprise only. SAML sessions last 24 hours. | GA |

## Hosting & Deployments

| Capability | What it does | Plan or cost | Maturity |
|---|---|---|---|
| **Deployment Retention** | Configurable auto-deletion policies for canceled, errored, preview, and production deployments to limit exposure of stale builds and meet compliance needs. Deleted deployments stay restorable for a 30-day recovery window; the last 10 deployments and last 20 ready production/non-production deployments are exempt. | Unlimited retention by default on all plans, with configurable policies available at the project level. No separate charge documented. | GA |
| **Git-Connected Deployments (CI/CD)** | Connects a Git repository (GitHub, GitLab, Bitbucket, Azure DevOps) and builds plus deploys on every push, with automatic framework detection and zero-config infrastructure output. Includes unlimited deployments, build logs, and deploy hooks/webhook triggers. | Included on all plans (Hobby, Pro $20/user/mo, Enterprise custom). Unlimited deployments on all plans. Webhook Triggers are Pro/Enterprise only. | GA |
| **Instant Rollback** | Immediately repoints production domains to a previously production-aliased deployment for fast incident recovery, disabling production domain auto-assignment until you undo it. Env vars are not re-applied and cron jobs revert to the rolled-back deployment's state. | Included on all plans. Hobby can only roll back to the immediately previous deployment; Pro/Enterprise can roll back to any eligible previously-aliased production deployment. | GA |
| **Microfrontends** | Splits one user-facing app across multiple independently deployed Vercel projects (monorepo or polyrepo) joined by a shared microfrontends.json path-routing config, with a local dev proxy and Toolbar routing overrides. Vercel handles cross-project routing and navigation optimization on the global network. | Hobby and Pro both include 2 microfrontend projects; Pro adds more at $250 per project per month. Routing requests: 50K/month included on Hobby; $2 per 1M requests on Pro. Enterprise custom. | GA |
| **Preview Deployments & Custom Environments** | Every non-production branch gets its own preview URL with an isolated environment; production comes from the production branch. Custom environments add named environments beyond preview/production with their own env vars and branch tracking. | Preview deployments included on all plans. Custom Environments: not available on Hobby, 1 on Pro, 12 on Enterprise. Preview Deployment Suffix (custom preview URL domain): $100/month on Pro, included on Enterprise. | GA |
| **Rolling Releases** | Routes a configurable fraction of production traffic to a canary deployment across staged percentages before full promotion, with Vercel-gathered comparison metrics (including Speed Insights) between base and canary. Controllable from the dashboard, CLI (vercel rolling-release start/complete), or REST API; abortable via Instant Rollback. | Pro and Enterprise only; Pro is limited to 1 project, Enterprise is custom. No separate per-use charge documented. Vercel strongly recommends pairing with Skew Protection. | GA |
| **Skew Protection** | Version-locks client and server by pinning framework-managed requests (assets, client navigations, prefetches, Server Actions) to the deployment that served the page using ?dpl=, x-deployment-id, or the __vdpl cookie. Supported zero-config on Next.js, SvelteKit, Qwik, Astro, and Nuxt. | Pro and Enterprise only; not available on Hobby. No separate charge. Default maximum age is 1 day, configurable up to your Deployment Retention limit (auto-extended to 60 days for Googlebot/Bingbot). Up to 12 allowed cross-site fetch domains. | GA |
| **Vercel Services** | Deploys multiple independently built frontends and backends (e.g. a Next.js app plus a FastAPI service) inside one Vercel project sharing a domain, env vars, firewall, and route table. Services are internal by default and exposed only through top-level rewrites; internal service-to-service calls use bindings. | Billed as normal Function, build, and CDN usage. A dedicated /docs/services/pricing page exists but I did not fetch it, so any service-specific limits or surcharges are unverified. Replaces the earlier experimentalServices config. | GA (supersedes experimental services) |

## Integrations

| Capability | What it does | Plan or cost | Maturity |
|---|---|---|---|
| **Marketplace Integrations** | Two integration types: Native Integrations, where you subscribe to a partner product entirely inside Vercel with billing on your Vercel invoice and no separate provider account, and Connectable Accounts, which link an existing third-party account and inject env vars. Covers storage, AI, observability, CMS, commerce, and testing categories; installable via dashboard or `vercel integration add` / `vercel install`. | Available on all plans. Native integration costs are set by the provider and billed through Vercel; Marketplace spend is excluded from Spend Management caps. Use Vercel Connect instead when you need delegated runtime credentials rather than a billed product. | GA |

## Networking

| Capability | What it does | Plan or cost | Maturity |
|---|---|---|---|
| **Secure Compute** | Provisions a dedicated VPC with its own static IP pair, NAT gateway, and CIDR block per network, optionally including the build container, with AWS VPC peering and active/passive cross-region failover. Supports Node.js, Python, Ruby, and Go — Edge runtime and middleware are not covered. | Enterprise only; custom pricing via your account team. Private Data Transfer $0.15/GB, charged only for traffic leaving the private network to the public internet (VPC-peered traffic is free). Limits: 100 projects per network, 50 VPC peering connections per network, up to 5s build provisioning delay. | GA (self-service network creation not enabled for all Enterprise teams) |
| **Static IPs (shared pool)** | Routes outbound function (and optionally build) traffic through fixed egress IP pairs in a shared VPC with subnet-level isolation, so you can allowlist Vercel on RDS, Cloud SQL, Mongo Atlas, Auth0, Stripe, or on-prem systems. Egress only — it does not provide a fixed inbound IP, and it does not apply to edge middleware. | Pro and Enterprise. $100 per project per month, plus Private Data Transfer at regional rates. Configured per project (applies to all environments). Not compatible with extended max duration beta, large functions beta, or container images beta; ignored if Secure Compute is enabled. | GA |

## Observability

| Capability | What it does | Plan or cost | Maturity |
|---|---|---|---|
| **Drains (Log Drains and beyond)** | Forwards observability data to custom HTTPS endpoints or native integrations (Dash0, Braintrust) and SIEMs. Data types: Logs, Traces (OTel), Speed Insights, Web Analytics, Vercel Connect events, and Audit Logs; destinations for audit logs include Amazon S3, Splunk, Datadog, and Panther. | Pro and Enterprise only (not Hobby or Pro Trial); Audit Log Drains are Enterprise-only. $0.50 per GB, same rate for every data type, billed on uncompressed JSON serialization size regardless of delivery encoding. | GA (replacing Custom SIEM Log Streaming) |
| **Observability & Observability Plus** | Framework-aware dashboards for Functions, Edge Requests, Middleware, External APIs, Fast Data Transfer, Image Optimization, ISR, PPR, Blob, Queues, AI Gateway, Workflows, Microfrontends, External Rewrites, Build Diagnostics, and Rolling Releases. Observability Plus adds latency, per-path breakdowns, the query builder, higher limits, and longer retention. | Base Observability is free on all plans. Observability Plus: paid Pro and Enterprise only at $1.20 per 1M events (Pro Trial excluded); you can exclude specific projects to control cost. Events counted at team level across Edge Requests, Function invocations, External API requests, Middleware invocations, and AI Gateway requests. | GA |
| **Query & Notebooks (and legacy Monitoring)** | A query console for building custom charts over Vercel usage, traffic, errors, and bot data, with results saved and organized into Notebooks. The older Monitoring product has been sunset for Pro and can no longer be enabled or accept new saved queries. | Requires Observability Plus (paid Pro/Enterprise, $1.20 per 1M events). Monitoring was sunset for Pro at the end of the Nov 2025 billing cycle; existing Enterprise users keep the deprecated version. Team-level IP address visibility in queries is toggleable for privacy compliance. | GA (Query/Notebooks); Monitoring deprecated |
| **Runtime Logs, Session Tracing & OpenTelemetry Tracing** | Streams runtime, build, and static logs, plus automatically instrumented traces covering CDN routing, middleware, caching, function lifecycle, and outbound fetch spans; @vercel/otel adds framework and custom spans. Session Tracing is enabled through the Vercel Toolbar for single-page or ongoing browser traces. | Runtime Logs retention: 1 hour Hobby, 1 day Pro, 3 days Enterprise — 30 days on Pro/Enterprise with Observability Plus. Session Tracing retention: 1 hour Hobby, 1 day Pro, 3 days Enterprise. Limits: 10 MB compressed trace data per request, spans over 1 MB compressed are dropped, Edge-runtime custom spans unsupported. | GA |

## Platform

| Capability | What it does | Plan or cost | Maturity |
|---|---|---|---|
| **Vercel for Platforms (multi-tenant)** | Serves many customer domains/subdomains from a single codebase and deployment (multi-tenant) or gives each customer their own project (multi-project), with automatic SSL for verified domains and wildcard domain support. Enterprise adds multi-tenant preview URLs like tenant1---project-git-branch.yourdomain.dev. | Custom domains: 50 on Hobby, unlimited on Pro and Enterprise (soft abuse limits of 100,000 domains/project on Pro and 1,000,000 on Enterprise, raisable on request). Multi-tenant preview URLs and custom SSL certificates are Enterprise-only. Compute, Firewall, and WAF included on all plans. | GA |

## Security

| Capability | What it does | Plan or cost | Maturity |
|---|---|---|---|
| **Advanced Deployment Protection** | Add-on bundle unlocking Password Protection, Private Production Deployments, and Deployment Protection Exceptions on a Pro team. Enabling it turns on all three features at once. | $150/month add-on for Pro; included by default on Enterprise. Must be active a minimum of 30 days before it can be disabled. Passport is not part of this add-on. | GA |
| **Attack Challenge Mode** | A one-toggle emergency mode that forces a security challenge on all browser visitors during a targeted attack, while automatically allowing verified bots (Googlebot, webhook providers) and your own Functions/Cron internal requests through. Does not harm SEO or search indexing. | Free on all plans. Mitigations are unlimited and requests blocked by Attack Mode do not count toward usage limits or incur any cost. | GA |
| **BotID** | An invisible CAPTCHA that issues a client-side challenge and validates it server-side with checkBotId(), with an optional Deep Analysis tier powered by Kasada that scores thousands of client signals and rotates detection logic per page load. Aimed at high-value routes such as checkout, signup, and APIs targeted by Playwright/Puppeteer-style bots. | Basic checks: free on all plans. Deep Analysis: Pro at $1 per 1,000 checkBotId() Deep Analysis calls; Enterprise custom; not available on Hobby. Only explicit checkBotId() calls are billed, not passive page views. | GA |
| **Deployment Protection (Vercel Authentication)** | Gates preview and production URLs behind authentication, with a protection method (Vercel Authentication, Passport, Password, Trusted IPs) and a scope (Standard Protection = everything except production domains, or All Deployments = including production). Protected Source Maps additionally gate .map requests behind Vercel Authentication. | Vercel Authentication with Standard Protection is available on all plans including Hobby. Protecting production domains ('All Deployments') requires Pro or Enterprise. Trusted IPs and production-only protection are Enterprise-only. Bypass for Automation and Shareable Links included. | GA |
| **Passport** | Protects deployments with your own identity provider (Microsoft Entra ID, Okta, any OIDC-compatible IdP) via a Vercel Connect OAuth application, setting a session cookie after IdP authentication. Server code can read verified visitor identity claims and forward signed Passport JWTs to other backends. | Enterprise-only feature; contact your Vercel account team for pricing. Not included in the Pro Advanced Deployment Protection add-on. | GA |
| **Platform DDoS Mitigation** | Always-on, automatic platform-wide firewall that blocks abnormal request volumes, TCP floods, and large-scale DDoS before traffic reaches your project. Runs first in the firewall rule chain, before WAF IP blocking, custom rules, and managed rulesets. | Free, unmetered, and enabled with no configuration on every plan including Hobby. | GA |
| **Vercel WAF (Custom Rules, IP Blocking, Rate Limiting)** | Project-level Web Application Firewall where you write custom match/action rules (allow, deny, challenge, bypass, rate limit), block IPs and CIDRs, and use JA3/JA4 TLS fingerprints, all manageable via dashboard, REST API, SDK, or Terraform. Includes live traffic monitoring, firewall alerts, and SIEM export via Drains. | Custom rules: 3 on Hobby, 40 on Pro, 1,000 on Enterprise. IP blocking: 3 / 100 / 1,000. System bypass rules: none on Hobby, 25 Pro, 100 Enterprise. Rate limiting: 1M requests/month included on Hobby; $0.50 per 1M requests on Pro; Enterprise custom. | GA |
| **WAF Managed Rulesets (OWASP, Bot Protection, AI Bots)** | Pre-built rule collections you toggle per project: the OWASP Core Ruleset with per-rule Log/Deny actions, a Bot Protection ruleset that serves a JS challenge to non-browser traffic, and an AI Bots ruleset that logs or denies AI crawlers. All are inactive by default and can be overridden with bypass custom rules. | Bot Protection and AI Bots managed rulesets are included on all plans. Per vercel.com/pricing, the OWASP Core Ruleset is listed as not available on Hobby or Pro and Custom on Enterprise — note the WAF docs describe it generically without a plan gate, so confirm OWASP availability for Pro with Vercel. | GA |

## Storage

| Capability | What it does | Plan or cost | Maturity |
|---|---|---|---|
| **Global Config (formerly Edge Config)** | An ultra-low-latency global key-value store replicated to every region, built for feature flags, A/B tests, emergency redirects, and IP blocklists without redeploying. Most reads return under 1 ms and P99 is within 15 ms; writes propagate globally in up to 10 seconds. | Hobby: 1 store total, 1 store per project, 100K reads and 100 writes/month included. Pro/Enterprise: unlimited stores, 3 per project. Reads $3.00 per 1M. Write pricing is stated inconsistently across docs — vercel.com/pricing says $1 per 100 writes while the Global Config limits page lists $5.00 with no unit; treat write pricing as unverified. Max store size 1 MB on every plan. Backup retention 7/90/365 days. | GA (renamed from Edge Config) |
| **Marketplace Storage (databases)** | Provisions third-party Postgres, Redis/KV, NoSQL, and vector databases (Neon, Supabase, Upstash, Prisma, AWS and others) from the Vercel dashboard or via `vercel install <provider>`, with credentials auto-injected as env vars. This replaced first-party Vercel Postgres/KV, which no longer appear as Vercel-owned storage products. | Provider-set pricing; native integrations are billed through your Vercel invoice. Available on all plans. Marketplace spend is excluded from Spend Management caps. | GA |
| **Vercel Blob** | S3-backed object storage with private (auth-required, served through your Functions) and public (direct URL) store modes, multipart uploads, conditional reads/writes via ETag, folders, and 11-nines durability. Stores can be created in any of 20 regions and connected to multiple projects. | Hobby included: 1 GB storage, 10K simple ops, 2K advanced ops, 10 GB transfer/month. Pro on-demand: $0.023/GB-month storage, $0.40 per 1M simple operations, $5 per 1M advanced operations, $0.05/GB Blob Data Transfer. Deletes are free. Pricing is regionalized; Blob Data Transfer averages ~3x cheaper than Fast Data Transfer. | GA |
