# Verification and sign-off record

The rule that prevents rework: a feature is "done" only when it has a row here. A row means it was verified at the named commit, by the named method, and does not need re-checking unless its files change again. When later work touches an area, re-verify only that area and update its row; everything else stands.

How things get verified in this project:
- Machine gates, every commit: `npx tsc --noEmit`, `npm run build` with blank env, `npm run lint`. A commit does not land red.
- Invariant review, every feature: the seven invariants in the project skill (store scoping, money gating, actor integrity, audit logging, human-in-the-loop on dollars, RLS coverage for new tables, blank-env build). Checked per screen during the work, recorded here.
- Owner sign-off: the owner uses the deployed preview or production and confirms the behavior. Until then a row says "verified, awaiting owner".

Where the record lives:
- This file: the per-area sign-off table below.
- docs/STATUS.md: the current state and what is left, updated in the same PR as the work.
- Pull requests on GitHub: the change-by-change record with verification notes in each description. PR #8 is the current one.
- activity_log in the database: the runtime audit of who did what in the app itself.

## Sign-off table

| Area | Verified at | Method | Status |
|---|---|---|---|
| Schema, seed, real bookings ledger (125 vendors) | ad89a59 (main) | Generator self-checks against the sheet's printed totals; loaded and used in production | Owner signed off (in production) |
| Capture, extract, confirm, save; feed; vendors; overdue; inventory; knowledge; ask; price signs; reports; reorder; maintenance; compliance; HR; schedule | main | Machine gates; invariant review; owner has used them on the live site | Owner signed off (in production) |
| Auth modes, per-store per-role RLS, Team page, in-app importer | dfef032 (main) | Machine gates; RLS policies reviewed table by table against auth_setup.sql | Owner signed off (in production) |
| Role-based home, command dashboard, notification bell, View as | 31b081a (PR #8) | Machine gates; invariant review (money gating on every KPI, store scoping on every query) | Verified, awaiting owner |
| Edit and delete on every data-entry screen; voided rows excluded everywhere | e042f3a (PR #8) | Machine gates; four independent agent passes each confirmed gating (owner and manager only), confirm-before-delete, audit rows, and voided_at filters per screen | Verified, awaiting owner; requires edit_delete.sql before deploy |
| Importer auto-detect (bookings vs weekly schedule), idempotent loads | e042f3a (PR #8) | Machine gates; dedupe keys reviewed (vendor name; employee plus date plus times) | Verified, awaiting owner |
| Redesign: app shell, navigation, design system, Today homes | de8459f, fe03392 (PR #8) | Machine gates; class-contract check (all existing class names kept); print rules re-checked for price signs | Verified, awaiting owner |
| Screen consistency pass and phone-first capture | e28828a, 7f46721, 27e1938, 7aa36d3 (PR #8) | Machine gates per batch; each agent confirmed presentation-only diffs with queries, gating, and handlers untouched | Verified, awaiting owner |
| edit_delete.sql completeness vs code | f30bc64 (PR #8) | Audited: the 13 tables with voidRow calls equal the 13 the migration adds voided_at/voided_by to; shift.department_id and the daily cadence covered; no read filters voided_at on shift, department, or any child table; migration is additive and idempotent | Verified; merge is safe once the owner runs it |
| Signed document URLs (private bucket, expiring links) | PR #8, 2026-07-07 | Machine gates; single call site confirmed (docUrls in src/lib/docs.ts); auth_setup.sql sets the bucket private; demo fallback to public URL confirmed in code | Verified, awaiting owner |
| Hard low-confidence gate (UI block plus DB trigger) | PR #8, 2026-07-07 | Machine gates; save-path review: editing clears the flag, remaining amber lines block save until acknowledged, ack stored on receiving_event, trigger refuses unacked low-confidence dollar lines; seeds confirmed to insert no receiving lines, so the trigger cannot break them | Verified, awaiting owner |
| Production audit fixes (money leaks, voided rows in totals, API hardening, admin escalation, import dedupe, auth_setup idempotency) | PR #8, 2026-07-07 | Seven-dimension audit (100 agents, three adversarial verifiers per finding); 50 raw findings triaged; every fix re-verified by hand against the code before landing; machine gates green after | Merged to production 2026-07-08 |
| Inventory-sheet importer (third auto-detected import type) | PR #9, 2026-07-08 | Parser exercised against replicas of all eight real Drive sheet shapes read live from the owner's files (multi-supplier SKU columns, repeated mid-sheet headers with forward-fill, junk quantities, order sheets excluded, section labels skipped, the bookings ledger correctly rejected): 28 assertions, all passing; machine gates green | Verified, awaiting owner; line notes need edit_delete.sql section 5 |
| Full Excel export with relationships (/api/export) | PR #9, 2026-07-08 | Table list checked against schema.sql column by column; pagination beyond 1000 rows; parent-scoped tables chunk their id lists; owner/manager gate mirrors /api/import; machine gates green | Merged to production 2026-07-08 |
| Value round: daily task check-offs, margin calculator, low-stock bell alert, record payment on Outstanding | PR #10, 2026-07-08 | Eight-angle code review (line-by-line, removed-behavior, cross-file, reuse, simplification, efficiency, altitude, conventions) on the diff before commit; all confirmed findings fixed the same pass, including a payment-method check-constraint violation, a month-end rollover bug in the recurrence math, the missing daily option in the recurrence dropdown, duplicated completion logic centralized into src/lib/tasks.ts and actor resolution into currentActorId(), parallelized bell and dashboard queries, and a duplicate-payment retry guard; machine gates green after | Verified, awaiting owner |
| Payments v2 (payouts, allocations, credit notes, categories, workflow fields) integration review | PR #11, 2026-07-20 | Four adversarial review angles over the 4,500-line upstream diff plus manual seam cross-checks (status consumers, export tables, RLS coverage, schema parity, RPC signatures); 13 confirmed defects fixed and re-verified; machine gates green; Drive re-checked (no new files; Lawson credit-card doc still present, owner reminded) | Verified, awaiting owner; requires payments_v2.sql rerun, credit_notes.sql, then auth_setup.sql rerun |
| Round 2: filing "both" end to end, overdue quick-form notes and filing, expected-ship-date floor, evergreen canvas | PR #12, 2026-07-26 | Machine gates; call-site review of every CONFIRMATION_FILING consumer and both ship-date paths (add blocks past dates, edit preserves history) | Merged to production 2026-07-26 |
| Payment edit (edit_payment RPC, both screens), invoice-paid-opens-payment loop | this PR | Machine gates; RPC reviewed for status re-derive on every allocated invoice and refusal of voided payments; both call sites re-validate actor and Other-method notes; multi-angle diff review | Verified, awaiting owner; requires payments_v2.sql rerun |
| Payrolls & Taxes category; finance-vendor form variant | this PR | Machine gates; seed idempotency (insert-where-missing per store); isFinanceDept scoped to name match; department appears in payout filters automatically | Verified, awaiting owner |
| Suggestions space (feedback table, page, triage route, export tab) | this PR | Machine gates; RLS: dev policy mirror in feedback.sql plus auth_setup 3a entry; actor-guarded writes; screenshot triage is best-effort and store-scoped through the caller's client; digest contains no dollar figures | Verified, awaiting owner; requires feedback.sql |
| Bookings importer v2 (header-driven) | this PR | Parser run against the owner's REAL annotated workbook: 125 vendors, 98 orders, 86 invoices, 33 payments, 2 credits; 8 structural assertions (referential integrity, id uniqueness, ISO dates, constraint-valid methods) all passing; idempotency mirrors v1 (skip existing vendors with children) | Verified, awaiting owner |
| Bell live refresh, KPI overflow fix, AI plain-text pass (aiText, temperatures, claude-sonnet-5 default), brand plaque | this PR | Machine gates; bell effect re-runs on pathname/focus/open/timer with cleanup; KPI length thresholds cover formatCAD six-digit totals; all four AI routes strip markdown server-side; plaque is CSS-only, print untouched | Verified, awaiting owner |
| Live-demo round: installments auto-spread, Statement view, sync phase 1 + uploads archive, evolved-header importer, vendor-creation standard, cross-links, picker categories, outstanding explainers, reference help, directory groups, Ask APP GUIDE + issue logging | PRs #16-#22, 2026-07-27 | Machine gates every commit; importer re-verified against the owner's real workbooks (130 vendors, methods, credits, remarks; structural assertions); statement balance semantics matched to owed rules; issue logging is deterministic, verbatim, member-attributed, enforced-mode only; owner exercised payment flows live during the walkthrough | Verified; owner used several flows live |

| Fresh-eyes vetting round (four expert reviews: UX, data model, reporting, engineering; 26 verified findings, all fixed) | PR #23, 2026-07-27 | Money math unified on invoiceTotal minus settlements across Today tiles, Charts, Ask, aging, and drill-down; post-dated remainders now aged and settlement-aware; HST report footnotes see-the-invoice rows; invoice Edit no longer writes the derived status (recompute RPC instead); overdue lists no-due-date invoices and takes partial amounts; double-pay hole closed (stale allocations pruned, missing invoice hard-errors); importer emits real partial payments (44 vs 33 on the real workbook), never fabricates full payments or self-destructing postdated statuses, hst null (see-invoice), year threaded, voided vendors surfaced not silently skipped, child-id idempotency heals partial runs; export pages with ORDER BY and READMEs the voided-rows rule; Ask logging imperative-only with dedupe; statement sorts charges-first on ties and scrolls on phones; credit form crash guarded; late-deliveries section behind the Today tile; Acting-as picker on the two screens whose errors demanded it; season year follows the calendar | Verified, awaiting owner |
| Domain and email integration record (docs/DOMAIN_EMAIL.md: inventory, urgent mailbox fixes, runbook) | PR #24, 2026-07-27 | Transcribed from the owner's three control-panel screenshots; plan cross-checked against the code (ALERT_EMAIL_FROM is what /api/alerts consumes; the Vercel custom domain needs no code change); no app code touched this round | Docs only; owner panel actions pending |
| Owner round 5 (filing locations, mandatory comments with N/A, tax explainer, section order) plus the autopilot's closed-ticket blind spot | PR #29, 2026-07-31 | Machine gates. The tax explainer was checked against the owner's own numbers before a line was written: on a 1,500 subtotal, 195 is 13 percent on top and 172.57 is the tax inside (1,327.43 x 1.13 = 1,500.00 exactly), so both figures he questioned were already correct and the fix was to show the working, not to change the arithmetic. Payment file locations are written after the engine commits rather than through the money RPC, so a failed write costs a path and never a dollar. The reopen rule for closed tickets is unit-tested against the board's REAL timestamps for all seven SCRUM tickets, including the nine-second gap on SCRUM-6 that made the grace window necessary; CI runs that test on every push. Reviewed by the money-reviewer subagent over the full diff | Verified, awaiting owner; requires filing_locations.sql |
| Closed-loop automation: tiered auto-merge, self-verifying /api/health, post-deploy watchdog, reviewer subagents, typecheck Stop hook | PR #28, 2026-07-28 | Machine gates. The tier classifier's path rules were unit-tested against twelve representative paths covering all three tiers, all correct, and smoke-tested against a real diff (it correctly refused to auto-merge its own workflow change). /api/health was called against a running production build with blank env and returned valid JSON without crashing. All scripts pass node --check; both workflows parse. The design and the reasoning for each limit are recorded in docs/LOOP_ENGINEERING.md; the load-bearing one is that .github/** and .claude/** are tier C, so the automation cannot alter its own limits or its own reviewers | Verified; auto-merge exercised only after the owner runs a tier A ticket through it |
| Department-first vendors navigation; owner's purchase-order and invoice field sets; tax-included invoices; one department sequence (the four SCRUM-9 comments) | PR #25, 2026-07-28 | Machine gates. Scouted first (six read-only passes mapping every affected surface), then reviewed by five adversarial lenses with an independent refutation pass per finding: 23 raw findings, 10 confirmed, all 10 fixed before commit. The money rules were verified numerically, not by inspection: 14 assertions covering tax-included vs tax-separate equivalence, embedded-HST arithmetic, goods-vs-owed, legacy rows with no tax_mode, and the fail-safe when a select omits tax_mode, all passing, with the SQL rule checked against the TypeScript rule case by case. Confirmed defects fixed: payments_v2.sql would have re-pinned the delivery constraint and undone the widening on a re-run; dropping "received" from the order statuses would have made every new order late forever (three filters now share orderIsOpen); the owner dashboard's open-orders tile counted a status nothing could write; switching an invoice to tax-included kept the 13%-on-top HST figure and would have overstated the input tax credit; the vendors page kept a department id and a half-typed vendor across a store switch; the season year was derived on add but not on edit; the bell quoted amount alone; the invoice edit form showed delivery and freight fields the add form hides for Property Maintenance and Payrolls & Taxes | Verified, awaiting owner; requires order_invoice_fields.sql |

| Owner round 6: field-level validation messages; vendor ordering profile (minimum order, summer timeline, order location, reorder status); autopilot tool-input fix | this PR, 2026-08-06 | Machine gates green (tsc, blank-env build, lint). The money gate was the thing worth checking here and it was wrong on the first pass: the minimum order is a dollar figure, and it was being rendered on the vendor page and the reorder list without a canSeeMoney check, which would have shown a staff member a figure the rest of the page hides. Now gated on read in both places, hidden in both forms, and not required of anyone who cannot see it (a mandatory invisible field is a form that cannot be submitted). The mutual exclusion of amount and "no minimum" is enforced twice on purpose, in the form and in a database check constraint, because the form is the layer that can be bypassed. Blank writes null rather than 0, since the constraint refuses a zero minimum and a staff member never sees the field to fill it. The new columns are all optional and the constraint was checked against the existing-row state (null amount, false flag) before it was written, so the 130 vendors already on file stay valid. RLS needs no change: these are columns on vendor, which is already in the store-scoped policy list in auth_setup 3a. schema.sql carries the same columns and constraint so a fresh database matches a migrated one. /api/health now checks all seven columns, which is what makes a skipped migration loud instead of a blank screen | Verified, awaiting owner; requires vendor_ordering_fields.sql |

| Health check names the script that fixes it (SCRUM-12) | this PR, 2026-08-07 | Machine gates green (tsc, blank-env build, lint). Verified by calling the route on a production build in four states, not by reading the code. (1) Blank env still answers `{"ok":true,"configured":false,"note":"No database configured. Nothing to check."}`, so the build-safe path is unchanged. (2) Against a stub standing in for PostgREST, set up as the live database actually is (vendor_ordering_fields.sql not run, filing_locations.sql not run, order_invoice_fields.sql run), and with NO secret, the response carries exactly the sentence the watchdog needed: "vendor is missing minimum_order_amount, no_minimum_order, summer_order_timeline, order_location, order_location_other, reorder_status, reorder_comments. Run supabase/vendor_ordering_fields.sql in the Supabase SQL editor, in the order given in docs/SUPABASE_SETUP.md. Safe to run again if it has already been run." The invoice check named filing_locations.sql ALONE and not order_invoice_fields.sql, which is the per-column mapping proving itself: a table waiting on two scripts now names the one it is actually waiting on. (3) With the secret, the same four sentences, since the missing-column answer is already the whole story and the raw PostgREST error added nothing worth withholding. (4) Pointed at a port with nothing on it, "database reachable" fails and the four columns checks fail with NO fix sentence, which is deliberate: a down database is not a migration somebody forgot, and sending the owner to the SQL editor for it would have been a wrong instruction delivered confidently. That case is what the `id` probe buys. No dollar figure, count, or row is exposed in any of the four; the fix names files already public in this repository. No money, RLS, or payments code touched, and the route only reads. Not verified against the real Supabase: this container has no route to it, so the live confirmation is the next post-deploy watch run after this merges | Verified against a stub; the underlying migration is still an owner action |

## Known-good environment facts (do not re-derive)
- The build must pass with blank env; auth and data helpers degrade to null rather than throw.
- schema.sql is a full rebuild; live databases get targeted ALTER snippets instead (edit_delete.sql is one).
- The documents bucket needs its storage policy or every capture fails RLS; shipped in schema.sql (dev) and auth_setup.sql (production).
- The Anthropic API key absence makes extract, ask, and reorder report it instead of failing.

## Open verifications
- vendor_ordering_fields.sql run against the live database (owner action; the health check has failed on "vendor columns present" after every deploy since 2026-08-06 and will keep failing until it runs, and the vendor ordering fields cannot save until then).
- edit_delete.sql run against the live database (owner action; everything in PR #8 that touches voided_at depends on it).
- Owner walkthrough of PR #8 preview: capture on a phone, edit and delete as manager, View as staff to confirm money stays hidden.
- Enforced-auth smoke test with one owner and one staff account after the login cutover.
